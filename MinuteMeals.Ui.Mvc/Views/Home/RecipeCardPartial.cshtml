@model Recipe

<div class="card">
    <div class="card-body">
        <h5 class="card-title"><a asp-controller="Recipe" asp-action="Details" asp-route-id="@Model.Id">@Model.Name</a></h5>
        <p class="card-text">@Model.CookingTimeInMinutes minute(s)</p>
    </div>
</div>